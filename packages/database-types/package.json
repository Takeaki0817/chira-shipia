{
  "name": "@smart-recipe/database-types",
  "version": "1.0.0",
  "type": "module",
  "main": "supabase.ts",
  "types": "supabase.ts",
  "scripts": {
    "build": "echo 'No build needed for TypeScript definitions'",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'No linting needed for TypeScript definitions'",
    "lint:fix": "echo 'No linting needed for TypeScript definitions'",
    "test": "echo 'No tests needed for TypeScript definitions'",
    "test:unit": "echo 'No tests needed for TypeScript definitions'",
    "test:watch": "echo 'No tests needed for TypeScript definitions'",
    "test:coverage": "echo 'No tests needed for TypeScript definitions'"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}